# Compatibility matrix

## OkHttp versions

The bridge is `compileOnly` against the **oldest** supported OkHttp so it cannot pick up
newer APIs and does not pull a version into the host app. The plugin allowlists the same
set at build time (`RecipeRegistry`). Hosts keep their own `implementation("okhttp:…")`.

| This library line | OkHttp | Notes |
| --- | --- | --- |
| unpublished (this repo) | 5.4.0, 5.5.0 | 5.4.0 is the compile floor; 5.5.0 is the sample/default |

When a new OkHttp **shares** internals with this family, add it to `RecipeRegistry`, the
CI matrix in `.github/workflows/pr.yml`, and this table (a **minor** of this library).

When a new OkHttp **breaks** internals (`initExchange` / `copy$okhttp$default` /
`ConnectInterceptor` shape / `Call.addEventListener`):

1. Raise the compile floor and drop the old OkHttp from the table (**minor**, if the
   published coordinate stays the same family, or **major** if the rewrite/bridge ABI
   is a new family).
2. Leave the previous library line published. Apps that cannot bump OkHttp stay on that
   line.
3. **Patch** the old line (`1.0.1` while `1.1.0` has a higher min) for bugfixes that
   still apply on the old internals. Do not mix two internals families in one AAR.

PR CI runs `:bridge:testDebugUnitTest` and `:sample:assembleDebug` against **every**
supported OkHttp (`-PokhttpVersion=`), plus `:sample:connectedDebugAndroidTest` and
`:sample:connectedMinifiedReleaseAndroidTest` on an API-30 emulator (same matrix).
The catalog workflow (`.github/workflows/catalog.yml`) runs only when
`gradle/libs.versions.toml` changes **and** the `okhttp =` pin moved. If the new
version matches the canonical ConnectInterceptor shape and JVM/plugin/assemble
tests pass, it commits goldens, `RecipeRegistry`, and the PR matrix back to the
branch (`scripts/pin-okhttp-version.sh`). A shape mismatch (new internals family)
fails without pinning — raise `okhttpMin` by hand. Host apps that consume the
published plugin still only **warn** on untested versions.

What the Cronet path does and does not do, exactly as the committed test suites prove it.
Every row cites at least one real test class/method from this repo's suites
(`BaselineSuite` / `CronetSuite` / `MinifiedSuite` on device; `CronetBridgeTest` /
`PolicyEngineTest` / `ResponseConverterTest` / `RequestConverterTest` /
`OkHttpBridgeCallbackTest` / `CronetProviderSelectionTest` / `PinTranslationTest` /
`EngineSetupTest` / `CacheHooksTest` / `SarieBridgeTest` /
`CallServerInterceptorRewriterTest` on the JVM).
These suites are the contract: if a row changes, a test changes.

Routing decisions are made **pre-send** by `PolicyEngine.shouldHandle` on the effective
`Interceptor.Chain` configuration — never after a Cronet request started. Every fallback
below means: stock OkHttp executes the exchange exactly as it would without this project.

| # | Behavior | Contract | Evidence |
| --- | --- | --- | --- |
| 1 | Application interceptors | Run normally — they sit above the swap point and observe the response the bridge returned. Adding interceptors via `newBuilder()` after install still runs them; clearing the interceptor list cannot disable the bridge. | `BaselineSuite.applicationInterceptorsStillRun`, `BaselineSuite.newBuilderInterceptorsRun`, `BaselineSuite.interceptorsClearCannotDisableBridge` |
| 2 | Redirects / follow-ups | `RetryAndFollowUpInterceptor` still functions above the swap: the Cronet path returns the 3xx with an empty body and never calls `followRedirect()`; each follow-up **re-enters the policy** and is served by Cronet again. | `CronetSuite.redirectFollowsAndReentersCronet`, `OkHttpBridgeCallbackTest.redirect surfaces 3xx with empty body and cancels request` |
| 3 | Custom authenticator (401 retry) | A custom `Authenticator` is allowed. A 401 is returned through Cronet so `RetryAndFollowUpInterceptor` calls `authenticator.authenticate(route = null, response)`. A custom `proxyAuthenticator` with no proxy is also allowed. A client with an explicit proxy or a non-DIRECT `proxySelector` still denies with `reason=proxy` (row 12). `Metrics.Reason.authenticator` is retired and is never produced. The device test asserts `Protocol.HTTP_2` on the local origin because that origin cannot negotiate HTTP/3; two Cronet hops and `route == null` are the proof. | `CronetSuite.authenticatorRetries401OverCronet`, `PolicyEngineTest.custom authenticator or proxyAuthenticator no longer denies` |
| 4 | Network interceptors | Network interceptors run. The allow branch registers a routed cycle and `proceed`s with no exchange, so OkHttp's own chain runs them and `CronetBridge.callServer` is the terminal hop. Sarie enforces the same scheme, host, and port, `proceed` exactly once, and no short-circuit, using stock's `IllegalStateException` messages (`must retain the same host and port` — a scheme change throws that same message — and `must call proceed() exactly once`). `chain.connection()` stays null. `Metrics.Reason.network_interceptors` is retired and is never produced. | `CronetSuite.networkInterceptorsSeeRequestResponseAndHttp3`, `CronetSuite.networkInterceptorHeaderReachesOrigin`, `CronetSuite.networkInterceptorUrlAndProceedGuards`, `MinifiedSuite.networkInterceptorsSeeRequestResponseAndHttp3`, `MinifiedSuite.networkInterceptorHeaderReachesOrigin`, `MinifiedSuite.networkInterceptorUrlAndProceedGuards`, `PolicyEngineTest.network interceptors are not denied`, `CronetBridgeTest.network interceptor host scheme or port change throws stock illegal state`, `CronetBridgeTest.network interceptor proceed twice throws stock exactly once`, `CronetBridgeTest.network interceptor short-circuit throws stock exactly once` |
| 5 | HTTPS cache (`client.cache != null`) | `client.cache` is not a deny. A Cronet 200 is stored without a TLS block. A hit has `handshake == null` and OkHttp fills `cacheResponse`. 304 revalidation stays on Cronet (the local origin is HTTP/2; the test does not require HTTP/3). Kill switch or uninstall makes Sarie entries miss and stock refetches with a handshake. `cache.evictAll()` drops them. `Metrics.Reason.cache` is retired and is never produced. | `CronetSuite.cacheableGetSecondRequestIsHitWithNullHandshake`, `CronetSuite.etagRevalidationStaysOnCronet`, `CronetSuite.onlyIfCachedServesSarieEntry`, `CronetSuite.killSwitchMissesSarieEntryAndStockRefetchHasHandshake`, `CronetSuite.evictAllDropsSarieEntries`, `PolicyEngineTest.client cache is not denied`, `CacheHooksTest.uninstall restores the stock checks`, `CacheHooksTest.enabled hooks skip an exhausted tls block and do not require a handshake` |
| 6 | Response metadata | No fabricated handshake, connection, or IP. `chain.connection()` stays null. The bridge-built response leaves `handshake`, `networkResponse`, and `cacheResponse` unset; OkHttp's cache interceptor fills `networkResponse` on every network response (with or without a cache, exactly as stock) and `cacheResponse` on a hit or revalidation (row 5). A cached HTTPS hit also has `handshake == null`, including `cacheResponse.handshake`. `sentRequestAtMillis` / `receivedResponseAtMillis` stay the bridge clocks on the network response: request handoff to Cronet (converter start) and header arrival (`onResponseStarted`). | `ResponseConverterTest.timestamps populated through the real callback and ordered`, `ResponseConverterTest.response request field is the original request`, `CronetSuite.cacheableGetSecondRequestIsHitWithNullHandshake`, `CronetSuite.borrowedDiskCacheReachesOriginTwice` |
| 7 | EventListener | `callEnd` still fires at **header return** while the body streams, so `callTimeout` bounds the header phase only. From the terminal hop only, Sarie also emits `requestHeadersStart` / `requestHeadersEnd` around the Cronet handoff, `requestBodyStart` / `requestBodyEnd` from the upload pump, `responseHeadersStart` when headers arrive, `responseHeadersEnd` after conversion, and `requestFailed` / `responseFailed` on errors. No connect, DNS, secure-connect, or response-body events. A network interceptor's `withReadTimeout` bounds the hop (header wait and body reads use that chain timeout). | `CronetSuite.eventListenerHeaderOrderAroundCronetHandoff`, `CronetSuite.networkInterceptorReadTimeoutAbortsStall`, `MinifiedSuite.eventListenerHeaderOrderAroundCronetHandoff`, `MinifiedSuite.networkInterceptorReadTimeoutAbortsStall`, `CronetBridgeTest.terminal hop emits requestHeadersStart before responseHeadersEnd`, `OkHttpBridgeCallbackTest.readTimeout stall cancels the request and throws timeout IOException`, `CronetSuite.readTimeoutStallAborts` |
| 8 | 3xx handling | Redirects surface as normal 3xx `Response`s for OkHttp's follow-up logic; the bridge never follows redirects inside Cronet. | `OkHttpBridgeCallbackTest.redirect surfaces 3xx with empty body and cancels request`, `CronetSuite.redirectFollowsAndReentersCronet` |
| 9 | 407 (proxy auth challenge) | A 407 on the Cronet path throws `ProtocolException("Received HTTP_PROXY_AUTH (407) code while not using proxy")`. It is not a `Response` and is not retried. Proxy-configured clients are still denied pre-send (row 12). | `CronetBridgeTest.407 response is rejected with ProtocolException` |
| 10 | Duplicate request headers | Duplicate request headers are one field. Values join with `", "` except `Cookie`, which uses `"; "` (the name match is case-insensitive, so `Cookie` and `cookie` are one field). The device test observes `X-Dup: [one, two]`. | `CronetSuite.hostHeaderAndDuplicatesDocumented`, `RequestConverterTest.repeated headers join with comma except Cookie with semicolon` |
| 11 | `Host` header | Always derived from the URL by the engine; an app-set `Host` override header is ignored on the Cronet path. | `CronetSuite.hostHeaderAndDuplicatesDocumented` |
| 12 | Proxy / proxySelector | Client with explicit `proxy`, or a `proxySelector` selecting a non-DIRECT proxy, is denied pre-send (`reason=proxy`). | `PolicyEngineTest.explicit proxy yields proxy`, `PolicyEngineTest.custom proxySelector yields proxy` |
| 13 | Certificate pins | `findMatchingPins` empty means the host is unaffected. Otherwise allow only when the engine is Sarie-built, the matching pins equal the installed set for that host, and none came from a single-label `*.host` pattern (`**.host` is installed with subdomains and is allowed when it equals that set). Otherwise `reason=pins`. SHA-1 pins and digit-dot hosts are not installed, so they cannot equal the installed set. A Cronet `-150` becomes `SSLPeerUnverifiedException("Certificate pinning failure!")` (that exact string, not stock's longer peer-chain message), is not retried, and is not handed to stock. `sdkPins` stays unused. | `PolicyEngineTest.certificate pins yields pins`, `PolicyEngineTest.sdkPins does not exempt certificate pins`, `PolicyEngineTest.pins for another host do not deny this host`, `PolicyEngineTest.sarie-built engine allows pins that match the installed set`, `PolicyEngineTest.derived client with different pins falls back`, `PolicyEngineTest.single label wildcard pin is denied on a sarie-built engine`, `PolicyEngineTest.double wildcard pin installed for the host is allowed`, `PinTranslationTest.sha1 and digit-dot hosts are not installed`, `CronetSuite.correctPinNegotiatesH3`, `CronetSuite.wrongPinIsPeerUnverifiedWithoutRetryOrStock`, `CronetSuite.singleLabelWildcardPinFallsBack`, `CronetBridgeTest.pin failure -150 becomes SSLPeerUnverifiedException and is not retried` |
| 14 | Socket factory / hostname verifier | Custom `socketFactory` class or non-default `hostnameVerifier` denied pre-send (`reason=socket_factory` / `reason=hostname_verifier`). | `PolicyEngineTest.custom socketFactory class yields socket_factory`, `PolicyEngineTest.custom hostnameVerifier yields hostname_verifier` |
| 15 | `Dns` | `chain.dns !== Dns.SYSTEM` denies with `reason=dns`. `Dns.SYSTEM` is allowed. Cronet performs its own resolution and never sees Dns-mapped addresses. | `PolicyEngineTest.custom dns yields dns`, `PolicyEngineTest.Dns SYSTEM does not deny`, `PolicyEngineTest.dns deny precedes pins` |
| 16 | Custom trust / TLS | Trust manager whose **class** differs from the captured platform default, OR whose `acceptedIssuers` fingerprint differs (same-class case), denied pre-send (`reason=trust`). Verdicts memoized per TM instance, bounded at 1024. | `PolicyEngineTest.custom sslSocketFactory and TM classes yield trust`, `PolicyEngineTest.custom TM with platform default class but different issuers yields trust (Metis B1)`, `PolicyEngineTest.trust verdicts memoized per TM instance and bounded at 1024` |
| 17 | WebSocket | Stays native: an `https://` upgrade attempt is denied pre-send (`reason=websocket`); a cleartext `ws://` upgrade hits the cleartext rule first. The upgrade request does reach the trampoline in OkHttp 5.5.0 — it is denied/fallback like any request. | `BaselineSuite.webSocketStaysNative`, `MinifiedSuite.webSocketUpgradeStillNative`, `PolicyEngineTest.websocket call yields websocket` |
| 18 | Cleartext / loopback / allowlist | Any `http://` denied (`reason=cleartext`); HTTPS to loopback hosts denied with `reason=cleartext` unless `DefaultPolicy(allowLoopbackHttps = true)`. Empty `allowedOrigins` (the `DefaultPolicy` default) and the `"*"` token admit every origin that passed the earlier rules; a non-empty set is exact-match (`host` = port 443, `host:port` is exact). | `BaselineSuite.cleartextGoesToStock`, `PolicyEngineTest.loopback https with allowLoopbackHttps yields allow`, `PolicyEngineTest.empty allowedOrigins admits every https origin`, `PolicyEngineTest.star token admits every https origin`, `PolicyEngineTest.allowlist port semantics - bare entry means default port 443, host with port is exact` |
| 19 | Kill switch (runtime) | System property `okhttp.cronet.enabled=false` (or `policy.enabled=false`) restores stock behavior immediately, no rebuild. | `BaselineSuite.killSwitchRestoresStock`, `CronetSuite.killSwitchRoutesStock`, `MinifiedSuite.killSwitchRestoresStock`, `PolicyEngineTest.kill switch off yields disabled` |
| 20 | Engine not installed | Falls back to stock with `reason=engine_missing`. | `CronetSuite.engineMissingFallsBackStock`, `PolicyEngineTest.null snapshot yields engine_missing` |
| 21 | Opt-out tag | `request.tag(CronetOptOut::class.java, CronetOptOut)` denies pre-send (`reason=tag_opt_out`). | `PolicyEngineTest.CronetOptOut tag yields tag_opt_out` |
| 22 | `H2_PRIOR_KNOWLEDGE` | Denied pre-send (`reason=h2_prior_knowledge`) — Cronet performs its own protocol negotiation. | `PolicyEngineTest.H2_PRIOR_KNOWLEDGE protocol yields h2_prior_knowledge` |
| 23 | HTTP/3 | **Public** h3 origin through the trampoline negotiates `Protocol.HTTP_3` on device. **Local-origin QUIC is blocked by Chromium's known-root policy** (`ERR_QUIC_CERT_ROOT_NOT_KNOWN`): locally-anchored CAs can never prove h3, regardless of chain completeness or builder knobs — the local origin deterministically serves h2. Server-side h3 capability of the local origin is proven separately via the Caddy access log. Netlog proof of the known-root rejection: `.omo/evidence/okhttp-cronet-transport/task-9/h3-blocker-netlog.json` (CERTIFICATE_VERIFIED followed by CONNECTION_CLOSE, alert 46). A second Sarie-built engine on the same storage path, with no QUIC hint, negotiates h3 on its first request with no QUIC hint (`CronetSuite.secondEngineReusesH3WithoutQuicHint`). Chromium also persists "QUIC broken" for a host after a failed QUIC handshake in that storage dir, with its usual backoff. | `CronetSuite.h3NegotiatedAgainstPublicOrigin`, `CronetSuite.h2LocalOriginWhileQuicBlocked`, `MinifiedSuite.publicOriginH3ThroughTrampoline`, `CronetSuite.secondEngineReusesH3WithoutQuicHint` |
| 24 | Compression | `reason=content_encoding` when `originalRequest` sets `Accept-Encoding` (even `gzip`: the app owns decoding), or the swap request's `Accept-Encoding` codings do not include `gzip`. No `Accept-Encoding` is allowed (the `Range` case: Chromium sends `identity` and `Content-Length` is kept). A swap value that lists `gzip`, including `br, gzip`, is allowed. The Sarie-built engine advertises `gzip, deflate` (brotli off). `zstd` is in `ENCODINGS_HANDLED_BY_CRONET` along with `br`, `deflate`, `gzip`, and `x-gzip`. When every response coding is in that set, the bridge strips `Content-Encoding` and `Content-Length` (one decode, engine-owned); identity keeps `Content-Length`; unknown or mixed encodings pass through. Successful brotli decode is on the borrowed engine that enables brotli. Wire-header diff against stock for the same request (recorded on cronet 500.0.2): Cronet adds an RFC 9218 `Priority` header, and `Accept-Encoding` is `gzip, deflate` instead of `gzip`; nothing else differs. | `CronetSuite.wireHeaderParityStockVersusCronet`, `CronetSuite.acceptEncodingCallerHeaderReplacedAndDecodeKeptConsistent`, `CronetSuite.rangeRequestGoesViaCronetWithIdentityEncoding`, `CronetSuite.compressionGzipDecoded`, `CronetSuite.brotliDecodedOnBorrowedEngine`, `PolicyEngineTest.originalRequest Accept-Encoding yields content_encoding`, `PolicyEngineTest.swap Accept-Encoding without gzip yields content_encoding`, `PolicyEngineTest.absent Accept-Encoding is allowed`, `PolicyEngineTest.swap Accept-Encoding listing gzip is allowed`, `ResponseConverterTest.all-Cronet-handled Content-Encoding strips encoding and length headers`, `ResponseConverterTest.identity passthrough keeps Content-Length` |
| 25 | Upload / streaming | POST bodies (1 MiB verified byte-exact) and incremental reads (chunked, ordered, caller byteCount honored) work on the Cronet path. | `CronetSuite.postUploadByteExact`, `CronetSuite.streamingLargeBody`, `OkHttpBridgeCallbackTest.streaming reads deliver chunks in order to completion` |
| 26 | Cancellation | 5-step ordered protocol via public `Call.addEventListener`: cancel-before-attach aborts before any engine start; cancel between attach and start delivers **exactly one** engine cancel; cancel mid-body aborts the pending read with `IOException("Canceled")`. Latency: delivery rides the OkHttp event path + the engine's cancel, not a poll loop; a cancel after full read is a no-op. | `CronetBridgeTest.cancel before attach aborts before any engine start`, `CronetBridgeTest.cancel between attach and start delivers exactly one engine cancel`, `CronetBridgeTest.cancel after headers mid-body aborts the read with Canceled`, `CronetSuite.cancelAfterHeadersAbortsBody` |
| 27 | Timeout model | Header wait and every body read bounded by `readTimeout` (stall aborts with a timeout IOException); `readTimeoutMillis == 0` is effectively infinite. `callTimeout` bounds only the header phase (row 7). | `CronetSuite.readTimeoutStallAborts`, `OkHttpBridgeCallbackTest.readTimeoutMillis zero means effectively infinite` |
| 28 | R8 survival | The `ConnectInterceptor` trampoline and the `CallServerInterceptor` prefix (`CronetBridge.callServer`) both survive R8 with no blanket keep of `okhttp3.internal.**` or `sarie.bridge.**`. Internals stay renamable; only the test-facing surface is pinned by name in `sample/proguard-rules.pro`. | `MinifiedSuite.callServerTrampolineSurvivesR8`, `MinifiedSuite.networkInterceptorsSeeRequestResponseAndHttp3`, `MinifiedSuite.cronetPathStillServes`, `MinifiedSuite.cleartextBypass` |
| 29 | Fallback shape | The deny path is still `initExchange` + `copy(exchange=)` + `proceed` and does not reference `ConnectInterceptor`. `callServer` returns null when `exchange != null`, and the prefix falls through so the stock body runs. A null exchange is the Sarie-routed call. | `CronetBridgeTest.fallback bytecode shape - initExchange and copy referenced and ConnectInterceptor absent`, `CallServerInterceptorRewriterTest.prefix is injected after the Kotlin preamble for every recipe variant` |
| 30 | Transport-failure retry | Compensates for the missing Exchange-based route retry: a pre-headers transport failure (`onFailed` -> `headersFuture` completed exceptionally with a `CronetException`) is retried **exactly once** with a fresh UrlRequest — only for idempotent methods (GET/HEAD/OPTIONS), only while the call is not canceled, and only when `client.retryOnConnectionFailure` is true. Header-wait timeouts and cancellations are terminal; nothing is ever retried after `onResponseStarted`. Each retry is counted in `Metrics.retries`. | `CronetBridgeTest.pre-headers transport failure retries once for GET and returns response`, `CronetBridgeTest.pre-headers transport failure twice for GET throws after exactly two attempts`, `CronetBridgeTest.non-idempotent POST pre-headers failure is terminal - no retry`, `CronetBridgeTest.cancel during the first attempt prevents the retry`, `CronetBridgeTest.retryOnConnectionFailure false disables the retry` |
| 31 | Engine ownership | `SarieBridge.install(context, client)` builds the engine and never calls `shutdown()`. Both `install` overloads touch `TrustBaseline` before returning, including when no provider is published, so the first request does not hash platform CAs. `configure` runs after the overridable defaults and before Sarie overwrites QUIC on, HTTP/2 on, brotli off, storage path `<noBackupFilesDir>/sarie-cronet`, `HTTP_CACHE_DISK_NO_HTTP` with size 0, and local-trust pin bypass off, and then appends pins. Overridable defaults are connection migration (`enableDefaultNetworkMigration` and `enablePathDegradationMigration`) and stale DNS (`enableStaleDns` and `preestablishConnectionsToStaleDnsResults`). A provider that rejects either does not fail setup. `configure` replaces stale DNS when it calls `setDnsOptions`. `configure` must not call `addPublicKeyPins`: Cronet only appends, and Sarie cannot remove pins `configure` already added. `install(engine)` is borrowed: no pins, compression and the HTTP cache are whatever the host built, and `disableCache()` is still called per request (row 32). `uninstall()` drops the reference and does not `shutdown()` either path. | `EngineSetupTest.configure cannot override bridge-owned settings`, `EngineSetupTest.rejected migration options do not fail setup`, `EngineSetupTest.configure can turn stale dns off`, `EngineSetupTest.rejected dns options do not fail setup`, `SarieBridgeTest.borrowed install is not sarie-built and records no pins or provider`, `SarieBridgeTest.second install replaces snapshot atomically and never closes engines`, `RequestConverterTest.convert disables the Cronet cache before build` |
| 32 | Cronet HTTP cache off | Both install paths call `UrlRequest.Builder.disableCache()` immediately before `build()`. A borrowed engine built with `HTTP_CACHE_DISK` still does not serve or store Sarie traffic: two GETs of `/cacheable-unique` (a new body per origin hit) both reach the origin. | `RequestConverterTest.convert disables the Cronet cache before build`, `CronetSuite.borrowedDiskCacheReachesOriginTwice` |
| 33 | Provider selection | Enabled providers only, in order `App-Packaged-Cronet-Provider`, `HttpEngine-Native-Provider`, `Google-Play-Services-Cronet-Provider`. Never `Fallback-Cronet-Provider` (Java `HttpURLConnection`). If none qualify, `install(context, client)` does not publish a snapshot, logs once, and every call falls back with `reason=engine_missing`. The chosen provider name and version are recorded on the snapshot when one is published. | `CronetProviderSelectionTest.app packaged wins over later providers including fallback`, `CronetProviderSelectionTest.disabled app packaged is skipped for HttpEngine`, `CronetProviderSelectionTest.disabled HttpEngine is skipped for Play Services`, `CronetProviderSelectionTest.fallback alone is rejected`, `CronetProviderSelectionTest.disabled providers and unknown names are skipped`, `CronetProviderSelectionTest.first enabled duplicate of the preferred name wins` |

## Non-goals (not implemented, so not tested)

Fabricating handshake or TLS session data, call-timeout over the body, response-body
`EventListener` events, proxies, bridging a custom `Dns`, desktop JVM support, and the
Java Cronet fallback (`Fallback-Cronet-Provider` / JavaCronetEngine) as a Cronet result.
Nothing here promises parity beyond the rows above.
