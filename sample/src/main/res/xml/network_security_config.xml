<?xml version="1.0" encoding="utf-8"?>
<!--
  Trust anchor for the Caddy HTTP/3 test origin (scripts/gen-certs.sh refreshes the
  raw resource copy). Scoped to the origin hosts; everything else keeps the system
  defaults. The embedded Cronet engine reads this config from the app manifest.
-->
<network-security-config>
    <domain-config>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <trust-anchors>
            <certificates src="@raw/caddy_root_ca" />
        </trust-anchors>
    </domain-config>
</network-security-config>
