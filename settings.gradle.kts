pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "okhttp-cronet-transport"

includeBuild("plugin-build")
include(":bridge")
include(":sample")
